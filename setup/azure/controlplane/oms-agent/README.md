### Description

Configure the oms agent in control plane cluster

### Usage

1) Initially ssh into relevant bastion and set the cluster context to Controlplane cluster

2) Execute `configure-oms-agent.sh` script
    ```bash
    bash configure-oms-agent.sh
    ```