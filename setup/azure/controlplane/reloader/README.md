## Reflector configuration

### Description
Configure the Reloader in control plane cluster

### Usage
1) Initially ssh into relevant bastion and set the cluster context to Controlplane cluster

2) Execute `configure-reflector.sh` script
    ```bash
    bash configure-reloader.sh
    ```