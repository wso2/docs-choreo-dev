## Linkerd Viz configuration

### Description

Configure the Linkerd Viz in control plane cluster

### Usage

1) Initially ssh into relevant bastion and set the cluster context to Controlplane cluster

3) Execute `configure-linkerd-viz.sh` script
    ```bash
    bash configure-linkerd-viz.sh
    ```