## Reflector configuration

### Description
Configure the Emberstack Reflector in routing cluster

### Prerequisites
1) Helm 3 installed in execution environment 

### Usage
1) Initially ssh into relevant bastion and set the cluster context to routing cluster

2) Execute `configure-reflector.sh` script
    ```bash
    bash configure-reflector.sh
    ```