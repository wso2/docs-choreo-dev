apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-headless
  namespace: <nginx-oidc-headless-service-namespace>
spec:
  clusterIP: None
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: zonesync
  selector:
    app: <nginx-oidc-headless-service-selector>
