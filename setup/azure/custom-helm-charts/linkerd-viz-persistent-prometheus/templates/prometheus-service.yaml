kind: Service
apiVersion: v1
metadata:
  name: prometheus
  namespace: {{.Values.namespace}}
  labels:
    linkerd.io/extension: viz
    component: prometheus
    namespace: {{.Values.namespace}}
spec:
  type: ClusterIP
  selector:
    linkerd.io/extension: viz
    component: prometheus
  ports:
    - name: admin-http
      port: 9090
      targetPort: 9090
