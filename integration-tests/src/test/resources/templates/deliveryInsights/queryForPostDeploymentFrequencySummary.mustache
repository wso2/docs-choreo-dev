query {
      deploymentFrequency (
            startTime: "{{startTime}}",
            endTime: "{{endTime}}",
            granularity: WEEKLY,
            isProd: true
        ) {
          totalDeployments,
          dataPoints,
          relativeChangeInDeployment,
          perfLevel
        }
      }
