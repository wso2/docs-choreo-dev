query {
      changeLeadTimes (
            startTime: "{{startTime}}",
            endTime: "{{endTime}}",
            granularity: WEEKLY,
            projectId: "{{projectId}}"
        ) {
          timestamp
          leadTime
        }
      }
