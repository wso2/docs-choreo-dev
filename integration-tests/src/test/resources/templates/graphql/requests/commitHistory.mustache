query {
      commitHistory(componentId: "{{componentId}}") {
          author {
            name
            date
            email
            avatarUrl
          }
        message
        sha
        isLatest
    }
  }
