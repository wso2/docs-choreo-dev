query {
  deploymentStatusByVersion(
    componentId: "{{componentId}}"
    versionId: "{{latestVersionId}}"
  ) {
    id
    sha
    completed_at
    started_at
    name
    status
    conclusion
    isAutoDeploy
    failureReason
    sourceCommitId
  }
}
