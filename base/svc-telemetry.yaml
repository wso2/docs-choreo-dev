apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: telemetry
  name: telemetry
  namespace: choreo-system
spec:
  ports:
  - name: "telemetry-port"
    port: 9090
    targetPort: 9090
  selector:
    choreo.component: telemetry