apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: billing-console
  name: billing-console
spec:
  ports:
  - name: bil-con-port
    port: 8080
    targetPort: bil-con-port
  selector:
    choreo.component: billing-console
