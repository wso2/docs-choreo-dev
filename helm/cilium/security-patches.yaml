apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    container.apparmor.security.beta.kubernetes.io/cilium-operator: runtime/default
  name: cilium-operator
spec:
  template:
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
