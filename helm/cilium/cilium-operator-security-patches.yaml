apiVersion: apps/v1
kind: Deployment
metadata:
  name: cilium-operator
  namespace: kube-system
spec:
  template:
    metadata:
      annotations:
        container.apparmor.security.beta.kubernetes.io/cilium-operator: runtime/default
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
