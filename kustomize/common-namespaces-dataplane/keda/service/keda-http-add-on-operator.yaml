apiVersion: v1
kind: Service
metadata:
  name: keda-http-add-on-operator-admin
  labels:
    app.kubernetes.io/instance: operator
  namespace: keda
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: operator
  ports:
  - name: admin
    protocol: TCP
    port: 9090
    targetPort: admin
