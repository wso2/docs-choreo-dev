apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - choreo-nginx
  - choreodp-system

patches:
  - path: linkerd-patch.yaml
    target:
      kind: Deployment

commonLabels:
  app: choreo-routing
