apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: cost-optimizer-cp
  name: cost-optimizer-cp
spec:
  ports:
    - name: cost-cp-port
      port: 8080
      targetPort: cost-cp-port
  selector:
    choreo.component: cost-optimizer-cp
