apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: copilot-datacollector
  name: copilot-datacollector
spec:
  ports:
    - name: data-col-port
      port: 8080
      targetPort: data-col-port
  selector:
    choreo.component: copilot-datacollector
