apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: choreo-gateway-extension-server
  name: choreo-gateway-extension-server
spec:
  ports:
    - name: extension-server
      port: 5005
      targetPort: extension-server
  selector:
    choreo.component: choreo-gateway-extension-server
