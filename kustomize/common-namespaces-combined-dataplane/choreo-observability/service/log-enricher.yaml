apiVersion: v1
kind: Service
metadata:
  name: log-enricher-service
  labels:
    app: log-enricher
    choreo.component: log-enricher
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: log-port
      protocol: TCP
      name: log-port
  selector:
    app: log-enricher
