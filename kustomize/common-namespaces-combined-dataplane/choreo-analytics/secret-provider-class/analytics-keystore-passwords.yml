apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: analytics-keystore-passwords
spec:
  secretObjects:
    - secretName: analytics-keystore-passwords
      type: Opaque
      data:
        - objectName: ALERT_API_PASSWORD
          key: ALERT_API_PASSWORD
        - objectName: TRUSTSTORE_PASSWORD
          key: TRUSTSTORE_PASSWORD
  parameters:
    objects: |
      array:
        - |
          objectName: analytics-keystore-pw-alert-api
          objectType: secret
          objectAlias: ALERT_API_PASSWORD
          objectVersion: "{{.ALERT_API_PASSWORD}}"
        - |
          objectName: analytics-truststore-password
          objectType: secret
          objectAlias: TRUSTSTORE_PASSWORD
          objectVersion: "{{.TRUSTSTORE_PASSWORD}}"
