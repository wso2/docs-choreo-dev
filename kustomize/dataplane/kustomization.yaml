apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base/clusterissuers
  - ../base/certificates
  - choreodp-system
  - crd
  - priorityclass

commonLabels:
  app: choreo-dataplane
