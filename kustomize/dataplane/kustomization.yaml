apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - choreodp-system
  - choreo-connect
  - traffic-manager

patches:
  - path: linkerd-patch.yaml
    target:
      kind: Deployment

commonLabels:
  app: choreo-dataplane
