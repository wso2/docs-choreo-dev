# TODO(pedrotorres): remove after implementing new routing table
apiVersion: v1
kind: Service
metadata:
  name: operator-admin
  labels:
    app.kubernetes.io/instance: operator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: operator
  ports:
  - name: admin
    protocol: TCP
    port: 9090
    targetPort: admin
