apiVersion: v1
kind: Service
metadata:
  name: interceptor-proxy
  labels:
    app.kubernetes.io/instance: interceptor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: interceptor
  ports:
  - name: proxy
    protocol: TCP
    port: 8080
    targetPort: proxy
