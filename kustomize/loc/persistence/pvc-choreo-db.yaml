apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    choreo.component: pvc-choreodb
  name: pvc-choreodb
spec:
  volumeMode: Filesystem
  storageClassName: file
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi