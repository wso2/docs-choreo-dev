configMapGenerator:
  - name: global-adapter-keystore-vol
    behavior: create
    files:
      - global-adapter.pem
  - name: mgw-enforcer-truststore
    behavior: create
    files:
      - certs/mg.pem
      - certs/publisher-truststore.pem
      - certs/wso2carbon.pem
      - certs/apim-service-root.pem
