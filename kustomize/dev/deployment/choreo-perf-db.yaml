apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    choreo.component: choreo-perf-db
  name: choreo-perf-db
spec:
  replicas: 1
  selector:
    matchLabels:
      choreo.component: choreo-perf-db
  strategy:
    type: RollingUpdate
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        choreo.component: choreo-perf-db
    spec:
      containers:
      - env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
           secretKeyRef:
            name: choreo-secret
            key: choreo_perf_db_password
        image: choreoipaas/choreo_perf_db:latest
        name: choreo-perf-db
        ports:
        - containerPort: 3306
        volumeMounts:
        - mountPath: /var/lib/mysql
          name: choreo-perf-db-claim
      volumes:
      - name: choreo-perf-db-claim
        persistentVolumeClaim:
          claimName: pvc-choreo-perf-db
