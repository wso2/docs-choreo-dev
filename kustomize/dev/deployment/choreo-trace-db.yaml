apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    choreo.component: choreo-trace-db
  name: choreo-trace-db
spec:
  replicas: 1
  selector:
    matchLabels:
      choreo.component: choreo-trace-db
  strategy:
    type: RollingUpdate
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        choreo.component: choreo-trace-db
    spec:
      containers:
      - env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
           secretKeyRef:
            name: choreo-secret
            key: choreo_trace_db_password
        image: choreoipaas/choreo_trace_db:latest
        name: choreo-trace-db
        ports:
        - containerPort: 3306
        volumeMounts:
        - mountPath: /var/lib/mysql
          name: choreo-trace-db-claim
      volumes:
      - name: choreo-trace-db-claim
        persistentVolumeClaim:
          claimName: pvc-choreo-trace-db
