apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    choreo.component: choreo-trace-db-claim
  name: pvc-choreo-trace-db
spec:
  volumeMode: Filesystem
  storageClassName: file
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi