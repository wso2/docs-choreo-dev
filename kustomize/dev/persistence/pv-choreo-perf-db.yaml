apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-choreo-perf-db
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 2Gi
  hostPath:
    path: /home/docker/choreo_perf_db
  persistentVolumeReclaimPolicy: Delete
  storageClassName: file
  volumeMode: Filesystem
#  claimRef:
#    name: pvc-choreo-perf-db
