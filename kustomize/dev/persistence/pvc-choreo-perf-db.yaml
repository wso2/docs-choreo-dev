apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    choreo.component: choreo-perf-db-claim
  name: pvc-choreo-perf-db
spec:
  volumeMode: Filesystem
  storageClassName: file
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi