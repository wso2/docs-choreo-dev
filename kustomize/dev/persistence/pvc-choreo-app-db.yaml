apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    choreo.component: choreo-app-db-claim
  name: pvc-choreo-app-db
spec:
  volumeMode: Filesystem
  storageClassName: file
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi