apiVersion: v1
kind: Service
metadata:
  name: default-backend
spec:
  selector:
    app: code-server-default-backend
  ports:
    - protocol: TCP
      name: http-port
      port: 8090
      targetPort: http-port
