apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - choreodp-system
  - code-server-default-backend

patches:
  - path: linkerd-patch.yaml
    target:
      kind: Deployment

commonLabels:
  app: choreo-workspace
