[ballerina.log]
level = "{{.LOG_LEVEL}}"

[choreo.choreo_limiter.config.subscriptionService]
url = "http://subscriptions:9090"

[choreo.choreo_limiter.config.clients]
timeout = 60.0
retryCount = 30
interval = 1.0
maxInterval = 120.0
backoffFactor = 2.0

[choreo.choreo_limiter.config.mizzenService]
url = "http://dp-mizzen:8080/api/v1/"
