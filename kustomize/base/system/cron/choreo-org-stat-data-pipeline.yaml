apiVersion: batch/v1
kind: CronJob
metadata:
  name: choreo-org-stat-data-pipeline #Name of the cron job
  labels:
    choreo.component: choreo-org-stat-data-pipeline #Label for the cron job
spec:
  schedule: "0 1 * * *" #Cron schedule to run at 1 AM everyday
  successfulJobsHistoryLimit: 3 #Number of successful job histories to keep
  concurrencyPolicy: Forbid #Concurrency policy to forbid concurrent execution of the job
  jobTemplate:
    spec:
      template:
        metadata:
          annotations:
            seccomp.security.alpha.kubernetes.io/pod: "runtime/default" #Annotation for seccomp security
            linkerd.io/inject: disabled #Annotation to disable linkerd injection
          labels:
            choreo.component: choreo-org-stat-data-pipeline #Label for the pod
        spec:
          automountServiceAccountToken: false #Automount service account token setting
          securityContext:
            seccompProfile:
              type: RuntimeDefault #Type of seccomp profile
          restartPolicy: Never #Pod restart policy
          terminationGracePeriodSeconds: 30 #Termination grace period in seconds
          containers:
            - name: choreo-org-stat-data-pipeline #Name of the container
              image: choreocontrolplane.azurecr.io/choreoipaas/choreo-org-stat-data-pipeline:latest #Docker image for the container
              securityContext:
                runAsUser: 10500 #User ID to run the container as
                runAsGroup: 10500 #Group ID to run the container as
                allowPrivilegeEscalation: false #Setting for privilege escalation
                readOnlyRootFilesystem: true #Setting for read only root filesystem
                capabilities:
                  drop:
                    - all #Capabilities to drop
              resources:
                limits:
                  cpu: 500m #CPU limit
                  memory: 1024Mi #Memory limit
                requests:
                  cpu: 300m #CPU request
                  memory: 512Mi #Memory request
              volumeMounts:
                - name: config #Name of the volume mount
                  mountPath: /home/ballerina/Config.toml #Path to mount the volume
                  subPath: Config.toml
                - name: secret-choreo-org-stat-data-pipeline
                  mountPath: "/mnt/csi/secret-choreo-org-stat-data-pipeline"
                  readOnly: true
          volumes:
            - name: config
              configMap:
                name: cm-file-choreo-org-stat-data-pipeline #Name of the config map
            - name: secret-choreo-org-stat-data-pipeline
              csi:
                driver: secrets-store.csi.k8s.io
                readOnly: true
                volumeAttributes:
                  secretProviderClass: secret-choreo-org-stat-data-pipeline
                nodePublishSecretRef:
                  name: csi-secret-store-azure
          serviceAccountName: sa-choreo-default
