apiVersion: batch/v1
kind: CronJob
metadata:
  name: choreo-platform-svc-cost-collector
  labels:
    choreo.component: choreo-platform-svc-cost-collector
spec:
  schedule: 30 23 * * *
  successfulJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 10
      completions: 1
      parallelism: 1
      template:
        metadata:
          annotations:
            seccomp.security.alpha.kubernetes.io/pod: "runtime/default"
            linkerd.io/inject: disabled # https://github.com/linkerd/linkerd2/issues/2833
          labels:
            choreo.component: choreo-platform-svc-cost-collector
        spec:
          automountServiceAccountToken: false
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          restartPolicy: OnFailure
          terminationGracePeriodSeconds: 30
          containers:
            - name: choreo-platform-svc-cost-collector
              image: choreocontrolplane.azurecr.io/choreoipaas/metered-usage-handler-crons:latest
              securityContext:
                runAsUser: 10500
                runAsGroup: 10500
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - all
              envFrom:
                - configMapRef:
                    name: cm-choreo-metered-usage-handler-platform-svc-cost-collector
                - configMapRef:
                    name: env-choreo-metered-usage-handler
                - secretRef:
                    name: secret-choreo-metered-usage-handler
              resources:
                limits:
                  cpu: 500m
                  memory: 450Mi
                requests:
                  cpu: 400m
                  memory: 350Mi
              volumeMounts:
                - name: config
                  mountPath: /home/ballerina/Config.toml
                  subPath: Config.toml
                - name: secret-choreo-metered-usage-handler
                  mountPath: "/mnt/csi/secret-choreo-metered-usage-handler"
                  readOnly: true
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: config
              configMap:
                name: cm-file-choreo-metered-usage-handler
            - name: secret-choreo-metered-usage-handler
              csi:
                driver: secrets-store.csi.k8s.io
                readOnly: true
                volumeAttributes:
                  secretProviderClass: secret-choreo-metered-usage-handler
                nodePublishSecretRef:
                  name: csi-secret-store-azure
            - name: tmp
              emptyDir: {}
          serviceAccountName: sa-choreo-default
