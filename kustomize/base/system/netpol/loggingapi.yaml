apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-loggingapi
spec:
  podSelector:
    matchLabels:
      choreo.component: loggingapi
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nginx-ingress-controller
  egress: [ ]
