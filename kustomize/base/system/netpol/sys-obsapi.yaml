apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-sys-obsapi
spec:
  podSelector:
    matchLabels:
      choreo.component: sys-obsapi
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nginx-ingress-controller
  egress: [ ]
