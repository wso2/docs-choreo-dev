apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-telemetry
spec:
  podSelector:
    matchLabels:
      choreo.component: telemetry
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nginx-ingress-controller
  egress: [ ]
