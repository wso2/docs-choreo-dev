apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-programanalyzer
spec:
  podSelector:
    matchLabels:
      choreo.component: programanalyzer
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nginx-ingress-controller
  egress: []
