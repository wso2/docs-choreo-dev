apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-telemetry-go
spec:
  podSelector:
    matchLabels:
      choreo.component: telemetry-go
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nginx-ingress-controller
  egress: [ ]
