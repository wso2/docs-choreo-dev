apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: analytics-subs
  name: analytics-subs
spec:
  ports:
    - name: service-port
      port: 9090
      targetPort: 9090
    - name: health-port
      port: 9093
      targetPort: 9093
  selector:
    choreo.component: analytics-subs
