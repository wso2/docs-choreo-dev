apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: alert-notification-service
  name: alert-notification-service
spec:
  ports:
    - name: an-port
      port: 9090
      targetPort: an-port
    - name: mgt-port
      port: 9092
      targetPort: mgt-port
  selector:
    choreo.component: alert-notification-service
