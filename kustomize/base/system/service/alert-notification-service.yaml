apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: alert-notification-service
  name: alert-notification-service
spec:
  ports:
  - name: an-port
    port: 9090
    targetPort: an-port
  selector:
    choreo.component: alert-notification-service
