apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: configuration-service
  name: configuration-service
spec:
  ports:
    - name: configsvc-port
      port: 8080
      targetPort: configsvc-port
    - name: healthz
      port: 8082
      targetPort: healthz
  selector:
    choreo.component: configuration-service
