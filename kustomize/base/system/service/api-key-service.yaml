apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: api-key-service
  name: api-key-service
spec:
  ports:
    - name: service-port
      port: 8080
      targetPort: service-port
  selector:
    choreo.component: api-key-service
