apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: frontend
  name: frontend
spec:
  ports:
    - name: frontend-port
      port: 8080
      targetPort: frontend-port
  selector:
    choreo.component: frontend
