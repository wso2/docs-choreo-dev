apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: shared-image-viewer
  name: shared-image-viewer
spec:
  ports:
    - name: http-port
      port: 3001
      targetPort: http-port

  selector:
    choreo.component: shared-image-viewer
