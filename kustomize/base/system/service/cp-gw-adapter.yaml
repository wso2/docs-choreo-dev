apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: cp-gw-adapter
  name: cp-gw-adapter
  namespace: choreo-system
spec:
  ports:
    - name: gw-adapter-port
      port: 8080
      targetPort: http-port

  selector:
    choreo.component: cp-gw-adapter
