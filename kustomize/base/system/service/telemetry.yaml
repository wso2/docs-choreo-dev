apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: telemetry
  name: telemetry
spec:
  ports:
    - name: telemetry-port
      port: 9090
      targetPort: telemetry-port
    - name: healthz
      port: 9092
      targetPort: healthz
  selector:
    choreo.telemetry: enabled
