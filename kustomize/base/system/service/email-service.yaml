apiVersion: v1
kind: Service
metadata:
  name: email-service
  labels:
    choreo.component: email-service
spec:
  ports:
    - name: email-port-http
      port: 443
      protocol: TCP
      targetPort: 8082
  selector:
    choreo.component: email-service
