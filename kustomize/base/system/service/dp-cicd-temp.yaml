apiVersion: v1
kind: Service
metadata:
  name: dp-cicd-temp
  labels:
    choreo.component: dp-cicd-temp
    choreo.dataplane: "true"
spec:
  ports:
    - name: dp-cicd-port-http
      port: 80
      protocol: TCP
      targetPort: http-port
  selector:
    choreo.component: dp-cicd-temp
