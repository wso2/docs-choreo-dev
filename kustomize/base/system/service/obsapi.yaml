apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: obsapi
  name: obsapi
spec:
  ports:
  - name: obsapi-port
    port: 9095
    targetPort: obsapi-port
  selector:
    choreo.component: obsapi
