apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: configuration-schema-service
  name: configuration-schema-service
spec:
  ports:
    - name: config-schema-port
      port: 80
      protocol: TCP
      targetPort: http-port
  selector:
    choreo.component: configuration-schema-service
