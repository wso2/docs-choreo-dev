apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: workflow-mgt
  name: workflow-mgt
spec:
  ports:
    - name: service-port
      port: 9082
      targetPort: service-port
    - name: health-port
      port: 9050
      targetPort: health-port
  selector:
    choreo.component: workflow-mgt
