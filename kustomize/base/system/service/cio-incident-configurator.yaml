apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: cio-incident-configurator
  name: cio-incident-configurator
spec:
  ports:
    - name: service-port
      port: 8080
      targetPort: service-port
  selector:
    choreo.component: cio-incident-configurator
