apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: testbase
  name: testbase
spec:
  ports:
    - name: tb-sport
      port: 9090
      targetPort: tb-sport
  selector:
    choreo.component: testbase
