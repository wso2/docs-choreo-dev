apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: declarative-api
  name: declarative-api
spec:
  ports:
    - name: service-port
      port: 8080
      targetPort: service-port
  selector:
    choreo.component: declarative-api
