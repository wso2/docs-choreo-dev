apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: declarative-api
  name: declarative-api
spec:
  ports:
    - name: declarativeapi-port
      port: 8080
      targetPort: declarativeapi-port
  selector:
    choreo.component: declarative-api
