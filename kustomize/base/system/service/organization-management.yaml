apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: organization-management
  name: organization-management
spec:
  ports:
    - name: http
      port: 9090
      targetPort: http
    - name: health-public
      port: 9092
      targetPort: health-public
    - name: grpc
      port: 8080
      targetPort: grpc

  selector:
    choreo.component: organization-management
