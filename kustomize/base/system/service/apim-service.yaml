apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: apim-service
  name: apim-service-svc
spec:
  type: ClusterIP
  ports:
    - name: http-port
      port: 9090
      targetPort: 9090
  selector:
    choreo.component: apim-service
