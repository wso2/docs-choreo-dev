apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: moesif-service
  name: moesif-service
spec:
  ports:
    - name: service-port
      port: 8080
      targetPort: service-port
    - name: healthz
      port: 9092
      targetPort: healthz

  selector:
    choreo.component: moesif-service