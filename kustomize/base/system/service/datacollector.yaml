apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: datacollector
  name: datacollector
spec:
  ports:
  - name: collector-port
    port: 9090
    targetPort: collector-port
  - name: healthz
    port: 9092
    targetPort: healthz
  selector:
    choreo.component: datacollector
