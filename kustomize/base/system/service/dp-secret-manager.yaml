apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: dp-secret-manager
    choreo.dataplane: "true"
  name: dp-secret-manager
spec:
  ports:
  - name: dp-secret-manager-port
    port: 80
    protocol: TCP
    targetPort: http-port
  selector:
    choreo.component: dp-secret-manager
    choreo.dataplane: "true"
