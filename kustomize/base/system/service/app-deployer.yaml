apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: app-deployer
  name: app-deployer
spec:
  ports:
    - name: app-deployer-port
      port: 8080
      protocol: TCP
      targetPort: grpc-port
  selector:
    choreo.component: app-deployer
