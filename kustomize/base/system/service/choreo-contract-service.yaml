apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: contract-service
  name: contract-service
spec:
  ports:
    - name: http-port
      port: 8080
      targetPort: http-port
  selector:
    choreo.component: contract-service
