apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: multivariate-anomaly-detector-notification-manager
  name: multivariate-anomaly-detector-notification-manager
spec:
  ports:
  - name: nm-port
    port: 9090
    targetPort: nm-port
  - name: healthz
    port: 9092
    targetPort: healthz
  selector:
    choreo.component: multivariate-anomaly-detector-notification-manager
