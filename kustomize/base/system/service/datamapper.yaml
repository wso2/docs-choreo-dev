apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: datamapper
  name: datamapper
spec:
  ports:
    - name: dm-port
      port: 5000
      protocol: TCP
      targetPort: dm-port
  selector:
    choreo.component: datamapper
