apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: sts-mgt-service
  name: sts-mgt-service
spec:
  ports:
    - name: service-port
      port: 3000
      targetPort: service-port
  selector:
    choreo.component: sts-mgt-service
