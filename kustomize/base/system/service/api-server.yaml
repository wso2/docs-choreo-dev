apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: api-server
  name: api-server
spec:
  ports:
    - name: api-server-port
      port: 80
      protocol: TCP
      targetPort: http-port
  selector:
    choreo.component: api-server
