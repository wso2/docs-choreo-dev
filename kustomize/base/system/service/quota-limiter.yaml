apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: quota-limiter
  name: quota-limiter
spec:
  ports:
    - name: grpc-port
      port: 9090
      targetPort: grpc-port
    - name: healthz
      port: 9092
      targetPort: healthz

  selector:
    choreo.component: quota-limiter
