apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment
  - service
  - hpa
  - pdb
  - rbac
  - component/linker
  - cron
  - config

namespace: choreo-system

patches:
- path: workspace-manager-port-skip-patch.yaml
  target:
    kind: Deployment
    name: workspace-manager
