apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    checkov.io/skip3: CKV_K8S_38=Required by linkerd
  labels:
    choreo.component: alert-configuration-service
  name: alert-configuration-service
spec:
  replicas: 1
  selector:
    matchLabels:
      choreo.component: alert-configuration-service
  strategy: {}
  template:
    metadata:
      labels:
        choreo.component: alert-configuration-service
        antiaffinity: alert-configuration-service
      annotations:
        linkerd.io/inject: enabled
        config.alpha.linkerd.io/proxy-wait-before-exit-seconds: "60"
    spec:
      automountServiceAccountToken: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - env:
            - name: ALERT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: secret-alert-configuration-service
                  key: ALERT_CHOREO_DB_PASSWORD
          envFrom:
            - configMapRef:
                name: env-alert-configuration-service
            - configMapRef:
                name: cm-alert-configuration-service
          image: choreocontrolplane.azurecr.io/choreoipaas/alert-configuration-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 10500
            runAsGroup: 10500
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          name: alert-configuration-service
          volumeMounts:
            - name: secret-alert-configuration-service
              mountPath: "/mnt/csi/secret-alert-configuration-service"
              readOnly: true
          ports:
            - containerPort: 8080
              name: an-port
              protocol: TCP
            - containerPort: 9091
              name: health
              protocol: TCP
          readinessProbe:
            initialDelaySeconds: 5
            timeoutSeconds: 15
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health
              path: /health/readiness
          livenessProbe:
            initialDelaySeconds: 10
            timeoutSeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health
              path: /health/liveness
          resources:
            limits:
              cpu: 500m
              memory: 600Mi
            requests:
              cpu: 300m
              memory: 500Mi
      volumes:
        - name: secret-alert-configuration-service
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: secret-alert-configuration-service
            nodePublishSecretRef:
              name: csi-secret-store-azure
      restartPolicy: Always
      serviceAccountName: sa-choreo-default
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: antiaffinity
                    operator: In
                    values:
                      - alert-configuration-service
              topologyKey: kubernetes.io/hostname
