apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    checkov.io/skip1: CKV_K8S_38=Required by LinkerD
  labels:
    choreo.component: cp-rrs
  name: cp-rrs
spec:
  replicas: 1
  selector:
    matchLabels:
      choreo.component: cp-rrs
  strategy: { }
  template:
    metadata:
      labels:
        choreo.component: cp-rrs
        antiaffinity: cp-rrs
      annotations:
        linkerd.io/inject: enabled
        config.alpha.linkerd.io/proxy-wait-before-exit-seconds: "60"
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cp-rrs
          image: choreocontrolplane.azurecr.io/choreoipaas/choreo-cp-rrs:latest
          env:
            - name: CP_RRS_ASB_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: secret-cp-rrs
                  key: CP_RRS_ASB_CONNECTION_STRING
          ports:
            - containerPort: 9095
              name: service-port
            - containerPort: 9091
              name: health-port
          readinessProbe:
            initialDelaySeconds: 60
            timeoutSeconds: 15
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health-port
              path: /readiness
          livenessProbe:
            initialDelaySeconds: 30
            timeoutSeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health-port
              path: /liveness
          resources:
            limits:
              cpu: 300m
              memory: 768Mi
            requests:
              cpu: 100m
              memory: 500Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 10500
            runAsGroup: 10500
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config
              mountPath: /home/ballerina/Config.toml
              subPath: Config.toml
            - name: secret-cp-rrs
              mountPath: "/mnt/csi/secret-cp-rrs"
              readOnly: true
      restartPolicy: Always
      volumes:
        - name: config
          configMap:
            name: cm-file-cp-rrs
        - name: secret-cp-rrs
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: secret-cp-rrs
            nodePublishSecretRef:
              name: csi-secret-store-azure
      serviceAccountName: sa-choreo-default
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: antiaffinity
                    operator: In
                    values:
                      - cp-rrs
              topologyKey: kubernetes.io/hostname
