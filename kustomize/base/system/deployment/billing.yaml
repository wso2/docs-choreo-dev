apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    reloader.stakater.com/auto: "true"
    seccomp.security.alpha.kubernetes.io/pod: "docker/default"
    checkov.io/skip2: CKV_K8S_43=False positive as the image tag gets updated to component git hash when deploying
  labels:
    choreo.component: billing
  name: billing
spec:
  automountServiceAccountToken: false
  replicas: 1
  selector:
    matchLabels:
      choreo.component: billing
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        choreo.component: billing
      annotations:
        linkerd.io/inject: enabled
        config.alpha.linkerd.io/proxy-wait-before-exit-seconds: "60"
        seccomp.security.alpha.kubernetes.io/pod: "docker/default"
    spec:
      securityContext:
        allowPrivilegeEscalation: false
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      containers:
        - image: choreocontrolplane.azurecr.io/choreoipaas/choreo-billing:latest
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 11000
            runAsGroup: 12000
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
                - NET_RAW
          name: billing
          envFrom:
            - configMapRef:
                name: env-billing
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: secret-subscriptions
                  key: MSSQL_PASSWORD
          ports:
            - containerPort: 9090
              name: http
              protocol: TCP
            - containerPort: 9091
              name: health-internal
              protocol: TCP
            - containerPort: 9092
              name: health-public
              protocol: TCP
          readinessProbe:
            initialDelaySeconds: 15
            timeoutSeconds: 15
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health-internal
              path: /readiness
          livenessProbe:
            initialDelaySeconds: 20
            timeoutSeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            successThreshold: 1
            httpGet:
              port: health-internal
              path: /liveness
          resources:
            limits:
              cpu: 500m
              memory: 768Mi
            requests:
              cpu: 300m
              memory: 512Mi
          volumeMounts:
            - name: secret-subscriptions
              mountPath: /mnt/csi/secret-subscriptions
              readonly: true
      restartPolicy: Always
      volumes:
        - name: secret-subscriptions
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: secret-subscriptions
            nodePublishSecretRef:
              name: csi-secret-store-azure
      serviceAccountName: sa-choreo-default
