apiVersion: v1
kind: Service
metadata:
  name: identity-server-svc
  labels:
    choreo.component: identity-server
spec:
  selector:
    deployment: wso2is
    choreo.component: identity-server
  ports:
  - name: servlet-http
    port: 9763
    targetPort: 9763
    protocol: TCP
  - name: servlet-https
    port: 9443
    targetPort: 9443
    protocol: TCP
