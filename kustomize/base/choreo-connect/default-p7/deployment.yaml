apiVersion: apps/v1
kind: Deployment
metadata:
  name: choreo-connect-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      choreo.component: choreo-connect-deployment-default-p7
  template:
    metadata:
      labels:
        choreo.component: choreo-connect-deployment-default-p7
        antiaffinity: choreo-connect-deployment-default-p7
    spec:
      initContainers:
      - name: init-adapter-service
        command: [ 'sh', '-c', 'echo -e "Checking for the availability of Adapter"; while ! nc -z adapter-default-p6 18000; do sleep 1; printf "-"; done; echo -e "  >> Adapter has started";' ]
      containers:
      - name: choreo-connect-enforcer
        env:
        - name: ADAPTER_HOST
          value: adapter-default-p7
      - name: choreo-connect-router
        env:
        - name: ADAPTER_HOST
          value: adapter-default-p7
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: antiaffinity
                  operator: In
                  values:
                  - choreo-connect-deployment-default-p7
              topologyKey: kubernetes.io/hostname
