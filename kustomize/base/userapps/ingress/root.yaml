apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: userapps-ingress-root
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - "domain.com"
    secretName: ingress-cert
  rules:
  - host: "domain.com"
