apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-choreo-appservice
spec:
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: file
  capacity:
    storage: 300Mi
  hostPath:
    path: /home/docker/choreo-appservice
  persistentVolumeReclaimPolicy: Delete