apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-choreo-mysql
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 20Gi
  hostPath:
    path: /home/docker/choreo-mysql
  persistentVolumeReclaimPolicy: Delete
  storageClassName: hostpath
  volumeMode: Filesystem