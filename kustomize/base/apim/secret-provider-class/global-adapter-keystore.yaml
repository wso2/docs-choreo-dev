apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: secret-cc-ga-keystore
spec:
  parameters:
    objects: |
      array:
        - |
          objectName: cc-GLOBAL-ADAPTER-KEYSTORE-KEY
          objectType: secret
          objectAlias: GLOBAL_ADAPTER_KEYSTORE_KEY
          objectVersion: "{{.GLOBAL_ADAPTER_KEYSTORE_KEY}}"
