apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: linker
  name: linker
spec:
  ports:
  - name: linker-secured-port
    port: 9090
    targetPort: linker-secured-port
  - name: linker-unsecured-port
    port: 9092
    targetPort: linker-unsecured-port

  selector:
    choreo.component: linker
