apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: default-backend
  name: default-backend
spec:
  ports:
  - name: dfbend-port
    port: 8080
    protocol: TCP
    targetPort: dfbend-port
  selector:
    choreo.component: default-backend