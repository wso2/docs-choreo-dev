apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: obsapi
  name: obsapi
spec:
  ports:
  - name: "obs-api-port"
    port: 9095
    targetPort: 9095
  selector:
    choreo.component: obsapi
