apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: loggingapi
  name: loggingapi
spec:
  ports:
  - name: "logging-api-port"
    port: 9097
    targetPort: 9097
  selector:
    choreo.component: loggingapi
