apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: capacity-planner
  name: capacity-planner
spec:
  ports:
    - name: capacity-planner-port
      port: 5000
      protocol: TCP
      targetPort: capacity-planner-port
  selector:
    choreo.component: capacity-planner

