apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: sys-obsapi
  name: sys-obsapi
spec:
  ports:
    - name: sys-obsapi-port
      port: 9098
      targetPort: sys-obsapi-port
    - name: healthz
      port: 9097
      targetPort: healthz
  selector:
    choreo.component: sys-obsapi
