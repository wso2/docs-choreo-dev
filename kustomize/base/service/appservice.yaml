apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: appservice
  name: appservice
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    choreo.component: appservice
