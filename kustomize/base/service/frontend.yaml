apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: frontend
  name: frontend
spec:
  ports:
  - name: "frontend-port"
    port: 8080
    targetPort: 8080
  selector:
    choreo.component: frontend
