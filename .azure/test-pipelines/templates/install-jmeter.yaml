steps:
  - script: |
      sudo apt-get update
      sudo apt-get install -y openjdk-11-jdk unzip
      echo "JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64" | sudo tee -a /etc/environment
      echo "PATH=\$PATH:\$JAVA_HOME/bin" | sudo tee -a /etc/environment
      source /etc/environment
      echo $JAVA_HOME
      echo $PATH
    displayName: 'Install Java and Set Environment Variables'

  - script: |
      wget https://downloads.apache.org//jmeter/binaries/apache-jmeter-5.6.3.tgz
      tar -xzf apache-jmeter-5.6.3.tgz
      sudo mv apache-jmeter-5.6.3 /opt/jmeter
      wget https://jmeter-plugins.org/files/JMeterPlugins-Standard-1.4.0.zip
      sudo unzip -o JMeterPlugins-Standard-1.4.0.zip -d /opt/jmeter
      wget https://jmeter-plugins.org/files/packages/jpgc-cmd-2.2.zip
      sudo unzip -o jpgc-cmd-2.2.zip -d /opt/jmeter
      java -cp /opt/jmeter/lib/ext/jmeter-plugins-manager-1.3.jar org.jmeterplugins.repository.PluginManagerCMDInstaller
      /opt/jmeter/bin/PluginsManagerCMD.sh install jpgc-synthesis=2.2
      /opt/jmeter/bin/PluginsManagerCMD.sh install jpgc-filterresults=2.2

    displayName: 'Install JMeter and Plugins'

  - script: |
      /opt/jmeter/bin/jmeter --version
    displayName: 'Check JMeter Version'
