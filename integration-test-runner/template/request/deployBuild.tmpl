mutation {
    deployDeploymentTrack(
        input: {
            componentId:  "{{.ComponentId}}"
            id: "{{.VersionId}}",
            imageId: "{{.ImageId}}",
            environmentId:"{{.EnvId}}",
            cron: "",
            cronTimezone: "",
        }
    )
}
