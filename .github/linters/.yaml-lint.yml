extends: default

ignore:
  # ignoring since the directory contains helm manifests
  - /tmp/lint/databases/templates/
  - /tmp/lint/cdp-helm-charts

rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 80
    level: warning
